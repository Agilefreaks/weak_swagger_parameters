require 'spec_helper'

describe WeakSwaggerParameters do
  it 'has a version number' do
    expect(WeakSwaggerParameters::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
